# Build output
bin/
obj/
dist/

# Secrets thật (không đẩy lên git) — chứa password PostgreSQL & JWT key thật
appsettings.Production.json
appsettings.*.Local.json
appsettings.Local.json

# SQLite lịch sử
*.db
*.db-shm
*.db-wal

# Linux
*.log
